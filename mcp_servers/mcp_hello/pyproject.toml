[project]
name = "mcp-hello"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastmcp>=2.0"]

[project.scripts]
mcp-hello = "hello:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["hello"]
