[project]
name = "mcps"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["mcp_servers/*"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "pytest-httpx>=0.36.2",
]

[tool.uv]
package = false
